numpy>=1.24
scikit-learn>=1.3
pandas>=2.0
streamlit>=1.28
tqdm>=4.65
pytest>=7.4

# Optional — only needed if you have offline network access to download
# pretrained weights BEFORE the timed scoring run, and want to swap the
# TF-IDF+SVD DenseEmbedder for real transformer embeddings (see commented
# alternative in scorer/embeddings.py):
# sentence-transformers>=2.2
